from splashkit import *

text = "SplashKit"

write_line(f"Processing the string: '{text}'")

# Get the length of the string
length = length_of(text)

write_line(f"The length of the string is: {length} characters.")
