using static SplashKitSDK.SplashKit;

string text = "SplashKit";

WriteLine("Processing the string: '" + text + "'");

// Get the length of the string
int length = LengthOf(text);

WriteLine($"The length of the string is: {length} characters.");
