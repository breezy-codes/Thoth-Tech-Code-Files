### Validate IP before converting to decimal

The following code shows examples of using [IPv4 to Dec](/api/networking/#ipv4-to-dec) to convert an IP address into decimal form, using input validation to match the regex pattern for an IPv4 address.